# React Component Library — Storybook Assessment

## Overview
Small component library with:
- `Input` — multi-type input (password visibility toggle, clearable)
- `Toast` — notification system with auto-dismiss and transitions
- `SidebarMenu` — slide-in right sidebar with nested items

## Run locally
```bash
npm install
npm run storybook
